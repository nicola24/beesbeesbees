// Create a Grub class, in pseudoclassical style, with:
var Grub = function() {
  // an age property that is set to 0
  this.age = 0;
  // a color property that is set to pink
  this.color = pink;
  // a food property that is set to jelly
  this.food = jelly;
};

// an eat method
Grub.prototype.eat = function() {
};

// *** create new bees base on Grub function  ***
// var newGrubA = new Grub();
// var newGrubB = new Grub();
// var newGrubC = new Grub();
